import React from "react";
import VideoStreamHandler from "../../websocket/VideoStream";

const VideoPage = () => {
  return (
    <div>
      <VideoStreamHandler />
    </div>
  );
};

export default VideoPage;
